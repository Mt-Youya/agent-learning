# AI model keys — used by app/api/chat/route.ts (server-side only)
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...   # optional, for claude-* models
DEEPSEEK_API_KEY=sk-...   # optional, for deepseek-* models

# Default model if client doesn't specify one
DEFAULT_MODEL=gpt-4o-mini
